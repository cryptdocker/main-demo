# Demo: APIs are mocked in src/demo/mockBackend.ts — no backend URL required.
# Add VITE_GOOGLE_CLIENT_ID only if you want to exercise Google OAuth UX against real credentials.
VITE_GOOGLE_CLIENT_ID=
